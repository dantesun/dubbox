# This workflow will build a package using Maven and then publish it to GitHub packages when a release is created
# For more information see: https://github.com/actions/setup-java/blob/main/docs/advanced-usage.md#apache-maven-with-a-settings-path

name: Maven Package

on:
  # release:
  #   types: [created]
  # push:
  #     branches:
  #       - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
    - uses: actions/checkout@v3
    - name: Fetch Project Version
      id: maven
      run: |
            {
              echo "version=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)"
            } >> $GITHUB_OUTPUT
    - name: Create Tag
      id: "tag_create"
      run: |
        {
          set -e
          git tag ${{steps.maven.outputs.version}}
          git push origin ${{steps.maven.outputs.version}}
        }
    - name: Set up JDK 1.8
      uses: actions/setup-java@v3
      with:
        java-version: '8'
        distribution: 'zulu'
        server-id: github # Value of the distributionManagement/repository/id field of the pom.xml
        settings-path: ${{ github.workspace }} # location for the settings.xml file
    - name: Build with Maven
      run: mvn -DskipTests -B package --file pom.xml
    - name: Generate Github Summary
      id: summary
      run:  |
            {
              echo "### Details "
              echo "| Variable   | Value       |"
              echo "| ---------- | ----------- |"
              echo "| version    | ${{steps.maven.outputs.version}}    |"
              echo "| checksum   | $(sha256sum dubbo/dubbo-${version}*.jar) |"
            } >> $GITHUB_STEP_SUMMARY
    - name: Release
      uses: softprops/action-gh-release@v1
      with:
        name: ${{steps.env.outputs.version}}
        files: |
          dubbo/dubbo-${{steps.maven.outputs.version}}.jar
          dubbo/dubbo-${{steps.maven.outputs.version}}-sources.jar
        prerelease: true
          

